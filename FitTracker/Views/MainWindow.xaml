<Window x:Class="FitTracker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:FitTracker.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="800">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5">
            <Label Content="FitTracker" FontSize="60px"/>
            <Button Command="{Binding NavigateCommand}" CommandParameter="Schedule" Margin="5">Weekly Schedule</Button>
        </StackPanel>
        <ContentControl prism:RegionManager.RegionName="MainRegion" Margin="5" />
    </DockPanel>

</Window>
